pipeline {
    agent { docker { image 'bitnami/git:latest' } }
    stages {
        stage('init') {
            steps {
                sh 'echo "LINUX NODE READY. SHOW INFO>>>"'
                sh 'git log -1 --pretty=%B'
                
            }
        }
        stage('build') {
            steps {
                sh 'uname -a'
            }
        }
    }
}