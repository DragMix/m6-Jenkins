pipeline {
    agent { docker { image 'ubuntu' } }
    stages {
        stage('init') {
            steps {
                sh 'echo "LINUX NODE READY. SHOW INFO>>>"'
                sh 'sudo apt install git'
                sh 'git log -1'
            }
        }
        stage('build') {
            steps {
                sh 'uname -a'
            }
        }
    }
}