pipeline {
    agent { docker { image 'alpine/git' } }
    stages {
        stage('init') {
            steps {
                sh 'echo "LINUX NODE READY. SHOW INFO>>>"'
                sh 'git log -1'
            }
        }
        stage('build') {
            steps {
                sh 'uname -a'
            }
        }
    }
}