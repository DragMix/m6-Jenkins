pipeline {
    agent { docker { image 'bitnami/git:latest' } }
    stages {
        stage('init') {
            steps {
                sh 'echo "LINUX NODE READY. SHOW INFO>>>"'
                def commit = sh(returnStdout: true, script: 'git log -1 --pretty=%B | cat')
                sh 'git log -1 --pretty=%B | cat'
                
            }
        }
        stage('build') {
            steps {
                sh 'uname -a'
            }
        }
    }
}